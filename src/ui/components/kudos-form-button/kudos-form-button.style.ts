import styled from "styled-components";
import { FloatButton } from "antd";

export const StyledKudosFormButton = styled(FloatButton)`
  .ant-float-btn-body {
    background-color: var(--primary-color);
  }

  .ant-float-btn-body:hover {
    background-color: var(--primary-color);
  }
`;
